services:
  gupea-dflow-import:
    image: docker.ub.gu.se/gupea-dflow-import:${GIT_REVISION}
    build:
      context: .
      args:
        - GIT_REPO_URL=${GIT_REPO_URL}
        - GIT_REVISION=${GIT_REVISION}
        - DSPACE_VERSION=${DSPACE_VERSION}
